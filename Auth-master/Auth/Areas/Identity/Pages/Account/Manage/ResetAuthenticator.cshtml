@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reset authenticator key";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Se você resetar a chave do seu autenticador, o aplicativo de autenticação não funcionará até que você o reconfigure.</strong>
    </p>
    <p>
        Este processo desabilita a autenticação de dois fatores (2FA) até que você verifique novamente o aplicativo de autenticação.
        Se você não completar a configuração do aplicativo de autenticação, poderá perder o acesso à sua conta.
    </p>
</div>

<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Resetar chave do autenticador</button>
    </form>
</div>
